spring:
  datasource:
    username: sa
    password: sa
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1

  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

